diff --git a/Cargo.lock b/Cargo.lock
index 5717717..c2dd5e2 100644
--- a/Cargo.lock
+++ b/Cargo.lock
@@ -2329,7 +2329,7 @@ version = "0.5.2"
 source = "registry+https://github.com/rust-lang/crates.io-index"
 checksum = "330c60081dcc4c72131f8eb70510f1ac07223e5d4163db481a04a0befcffa412"
 dependencies = [
- "libloading 0.7.4",
+ "libloading 0.8.4",
 ]
 
 [[package]]
@@ -2694,7 +2694,7 @@ source = "registry+https://github.com/rust-lang/crates.io-index"
 checksum = "39cab71617ae0d63f51a36d69f866391735b51691dbda63cf6f96d042b63efeb"
 dependencies = [
  "libc",
- "windows-sys 0.60.2",
+ "windows-sys 0.61.2",
 ]
 
 [[package]]
@@ -3019,6 +3019,16 @@ dependencies = [
  "time 0.1.45",
 ]
 
+[[package]]
+name = "fs2"
+version = "0.4.3"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "9564fc758e15025b46aa6643b1b77d047d1a56a1aea6e01002ac0c7026876213"
+dependencies = [
+ "libc",
+ "winapi 0.3.9",
+]
+
 [[package]]
 name = "fsevent"
 version = "2.1.2"
@@ -4494,7 +4504,7 @@ source = "registry+https://github.com/rust-lang/crates.io-index"
 checksum = "e310b3a6b5907f99202fcdb4960ff45b93735d7c7d96b760fcff8db2dc0e103d"
 dependencies = [
  "cfg-if 1.0.0",
- "windows-targets 0.48.5",
+ "windows-targets 0.52.6",
 ]
 
 [[package]]
@@ -4711,6 +4721,22 @@ dependencies = [
  "libc",
 ]
 
+[[package]]
+name = "managed-control-core"
+version = "1.0.0"
+dependencies = [
+ "base64 0.22.1",
+ "fs2",
+ "log",
+ "reqwest",
+ "serde 1.0.228",
+ "serde_json 1.0.118",
+ "sha2",
+ "sodiumoxide",
+ "url",
+ "uuid",
+]
+
 [[package]]
 name = "matches"
 version = "0.1.10"
@@ -7300,6 +7326,7 @@ dependencies = [
  "libxdo-sys",
  "mac_address",
  "magnum-opus",
+ "managed-control-core",
  "nix 0.29.0",
  "num_cpus",
  "objc",
@@ -7434,7 +7461,7 @@ dependencies = [
  "errno",
  "libc",
  "linux-raw-sys 0.11.0",
- "windows-sys 0.60.2",
+ "windows-sys 0.61.2",
 ]
 
 [[package]]
@@ -7491,7 +7518,7 @@ dependencies = [
  "security-framework 3.5.1",
  "security-framework-sys",
  "webpki-root-certs",
- "windows-sys 0.60.2",
+ "windows-sys 0.61.2",
 ]
 
 [[package]]
diff --git a/Cargo.toml b/Cargo.toml
index d909ec3..f75c4a8 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -21,6 +21,7 @@ name = "service"
 path = "src/service.rs"
 
 [features]
+managed-control = ["flutter", "dep:managed-control-core", "hbb_common/managed-control"]
 inline = []
 use_samplerate = ["samplerate"]
 use_rubato = ["rubato"]
@@ -44,6 +45,7 @@ screencapturekit = ["cpal/screencapturekit"]
 # See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
 
 [dependencies]
+managed-control-core = { path = "src/managed_control", optional = true }
 async-trait = "0.1"
 scrap = { path = "libs/scrap", features = ["wayland"] }
 hbb_common = { path = "libs/hbb_common" }
@@ -202,7 +204,7 @@ android-wakelock = { git = "https://github.com/rustdesk-org/android-wakelock" }
 
 [workspace]
 members = ["libs/scrap", "libs/hbb_common", "libs/enigo", "libs/clipboard", "libs/virtual_display", "libs/virtual_display/dylib", "libs/portable", "libs/remote_printer"]
-exclude = ["vdi/host", "examples/custom_plugin"]
+exclude = ["src/managed_control", "vdi/host", "examples/custom_plugin"]
 
 # Patch libxdo-sys to use a stub implementation that doesn't require libxdo
 # This allows building and running on systems without libxdo installed (e.g., Wayland-only)
diff --git a/flutter/lib/desktop/pages/desktop_setting_page.dart b/flutter/lib/desktop/pages/desktop_setting_page.dart
index 8cd640f..e23090b 100644
--- a/flutter/lib/desktop/pages/desktop_setting_page.dart
+++ b/flutter/lib/desktop/pages/desktop_setting_page.dart
@@ -2410,17 +2410,23 @@ class _AboutState extends State<_About> {
   @override
   Widget build(BuildContext context) {
     return futureBuilder(future: () async {
+      final managedNotice = await bind.mainGetCommon(key: 'managed-control-notice');
+      final managedSource = await bind.mainGetCommon(key: 'managed-control-source');
       final license = await bind.mainGetLicense();
       final version = await bind.mainGetVersion();
       final buildDate = await bind.mainGetBuildDate();
       final fingerprint = await bind.mainGetFingerprint();
       return {
+        'managedNotice': managedNotice,
+        'managedSource': managedSource,
         'license': license,
         'version': version,
         'buildDate': buildDate,
         'fingerprint': fingerprint
       };
     }(), hasData: (data) {
+      final managedNotice = data['managedNotice'].toString();
+      final managedSource = data['managedSource'].toString();
       final license = data['license'].toString();
       final version = data['version'].toString();
       final buildDate = data['buildDate'].toString();
@@ -2430,6 +2436,10 @@ class _AboutState extends State<_About> {
       return SingleChildScrollView(
         controller: scrollController,
         child: _Card(title: translate('About RustDesk'), children: [
+          if (managedNotice.isNotEmpty) ...[
+            Text(managedNotice),
+            TextButton(onPressed: () => launchUrlString(managedSource), child: const Text('Source Code')),
+          ],
           Column(
             crossAxisAlignment: CrossAxisAlignment.start,
             children: [
diff --git a/src/common.rs b/src/common.rs
index b875d54..b447e82 100644
--- a/src/common.rs
+++ b/src/common.rs
@@ -2271,6 +2271,10 @@ pub fn get_hwid() -> Bytes {
 
 #[inline]
 pub fn get_builtin_option(key: &str) -> String {
+    #[cfg(feature = "managed-control")]
+    if key == "disable-change-permanent-password" && Config::is_disable_change_permanent_password() {
+        return "Y".into();
+    }
     config::BUILTIN_SETTINGS
         .read()
         .unwrap()
diff --git a/src/flutter_ffi.rs b/src/flutter_ffi.rs
index 0777443..5d8ddef 100644
--- a/src/flutter_ffi.rs
+++ b/src/flutter_ffi.rs
@@ -2811,6 +2811,13 @@ pub fn main_get_printer_names() -> SyncReturn<String> {
 }
 
 pub fn main_get_common(key: String) -> String {
+    #[cfg(all(feature = "managed-control", not(any(target_os = "android", target_os = "ios"))))]
+    match key.as_str() {
+        "managed-control-notice" => return crate::managed_control::notice(),
+        "managed-control-source" => return managed_control_core::SOURCE_URL.into(),
+        _ => {}
+    }
+
     if key == "is-printer-installed" {
         #[cfg(target_os = "windows")]
         {
diff --git a/src/ipc.rs b/src/ipc.rs
index 68c987f..70ccffb 100644
--- a/src/ipc.rs
+++ b/src/ipc.rs
@@ -922,6 +922,8 @@ async fn handle(data: Data, stream: &mut Connection) {
                 if let Some(v) = value.get("privacy-mode-impl-key") {
                     crate::privacy_mode::switch(v);
                 }
+                #[cfg(feature = "managed-control")]
+                let value = { let mut value = value; crate::managed_control::preserve_options(&mut value); value };
                 Config::set_options(value);
                 allow_err!(stream.send(&Data::Options(None)).await);
             }
@@ -932,6 +934,8 @@ async fn handle(data: Data, stream: &mut Connection) {
         }
         Data::SyncConfig(Some(configs)) => {
             let (config, config2) = *configs;
+            #[cfg(feature = "managed-control")]
+            let (config, config2) = { let (mut config, mut config2) = (config, config2); crate::managed_control::preserve_config(&mut config, &mut config2); (config, config2) };
             let _chk = CheckIfRestart::new();
             Config::set(config);
             Config2::set(config2);
diff --git a/src/lib.rs b/src/lib.rs
index 49cb2b7..edd2d26 100644
--- a/src/lib.rs
+++ b/src/lib.rs
@@ -1,3 +1,5 @@
+#[cfg(all(feature = "managed-control", not(any(target_os = "android", target_os = "ios"))))]
+mod managed_control;
 mod keyboard;
 /// cbindgen:ignore
 pub mod platform;
diff --git a/src/platform/linux.rs b/src/platform/linux.rs
index 9a4bb37..efbbfb3 100644
--- a/src/platform/linux.rs
+++ b/src/platform/linux.rs
@@ -791,6 +791,8 @@ fn force_stop_server() {
 }
 
 pub fn start_os_service() {
+    #[cfg(feature = "managed-control")]
+    crate::managed_control::start();
     check_if_stop_service();
     stop_rustdesk_servers();
     stop_subprocess();
diff --git a/src/platform/macos.rs b/src/platform/macos.rs
index 2e68cf5..92896f0 100644
--- a/src/platform/macos.rs
+++ b/src/platform/macos.rs
@@ -729,6 +729,8 @@ pub fn lock_screen() {
 }
 
 pub fn start_os_service() {
+    #[cfg(feature = "managed-control")]
+    crate::managed_control::start();
     log::info!("Username: {}", crate::username());
     if let Err(err) = crate::ipc::start("_service") {
         log::error!("Failed to start ipc_service: {}", err);
diff --git a/src/server.rs b/src/server.rs
index 89a17a9..ed0938b 100644
--- a/src/server.rs
+++ b/src/server.rs
@@ -609,6 +609,8 @@ pub async fn start_server(is_server: bool, no_server: bool) {
         crate::platform::try_kill_broker();
         #[cfg(feature = "hwcodec")]
         scrap::hwcodec::start_check_process();
+        #[cfg(all(feature = "managed-control", target_os = "windows"))]
+        crate::managed_control::start();
         crate::RendezvousMediator::start_all().await;
     } else {
         match crate::ipc::connect(1000, "").await {
@@ -782,6 +784,18 @@ async fn sync_and_watch_config_dir(sync_done_tx: Option<tokio::sync::oneshot::Se
 
                 loop {
                     sleep(CONFIG_SYNC_INTERVAL_SECS).await;
+                    #[cfg(feature = "managed-control")]
+                    {
+                        if conn.send(&Data::SyncConfig(None)).await.is_ok() {
+                            if let Ok(Some(Data::SyncConfig(Some(configs)))) = conn.next_timeout(1000).await {
+                                let (config, config2) = *configs;
+                                if config2.options.get("managed-control-enabled").map(String::as_str) == Some("Y") {
+                                    let _chk = crate::ipc::CheckIfRestart::new();
+                                    crate::managed_control::merge_service_config(&config, &config2, &mut cfg0);
+                                }
+                            }
+                        }
+                    }
                     let cfg = (Config::get(), Config2::get());
                     let should_sync =
                         cfg != cfg0 || (is_root_config_empty && !cfg.0.is_empty());
